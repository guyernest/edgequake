/**
 * Placeholder resolver for Amplify Gen 2 custom queries.
 *
 * Returns null for all operations. Real resolvers will be
 * implemented in Phase 5 when the GraphQL API is wired to
 * the DynamoDB namespace table via AppSync VTL/JS resolvers.
 */
export function request(ctx) {
  return {};
}

export function response(ctx) {
  return null;
}
